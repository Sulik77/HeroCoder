const initialStaate = {};

export default function(state = initialState, action) {
  switch (action.type) {
    //   case ADD_TASK: {
    //     return {

    //     };
    //   }

    default:
      return state;
  }
}
